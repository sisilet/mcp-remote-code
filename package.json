{
  "name": "mcp-remote-code",
  "version": "2.1.0",
  "description": "Stdio MCP server for remote machine control over SSH with a jailed root directory",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "mcp-remote-code": "./build/index.js"
  },
  "files": [
    "build",
    "README.md",
    "README.zh-CN.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "postbuild": "node scripts/postbuild.mjs",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts test/tools/*.test.ts",
    "test:docker": "node --import tsx --test test/docker/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "diff": "^9.0.0",
    "ssh2": "^1.17.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/diff": "^7.0.2",
    "@types/node": "^20.0.0",
    "@types/ssh2": "^1.15.5",
    "tsx": "^4.19.0",
    "typescript": "^5.4.0"
  },
  "keywords": [
    "mcp",
    "ssh",
    "remote",
    "agent",
    "stdio"
  ],
  "license": "MIT"
}
